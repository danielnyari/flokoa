apiVersion: agent.flokoa.ai/v1alpha1
kind: ModelProvider
metadata:
  name: mock-provider
  namespace: flokoa-system
spec:
  apiKeySecretRef:
    name: mock-provider-api-key
    key: api-key
  openai:
    baseURL: http://llm-stub:8080/v1
