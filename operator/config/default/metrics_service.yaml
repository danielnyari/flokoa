apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: flokoa
    app.kubernetes.io/component: controller
    app.kubernetes.io/managed-by: kustomize
  name: flokoa-controller-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: flokoa
    app.kubernetes.io/component: controller
