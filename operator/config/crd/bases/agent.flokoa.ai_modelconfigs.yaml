---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modelconfigs.agent.flokoa.ai
spec:
  group: agent.flokoa.ai
  names:
    kind: ModelConfig
    listKind: ModelConfigList
    plural: modelconfigs
    singular: modelconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelRef.name
      name: Model
      type: string
    - jsonPath: .status.resolvedModel.provider
      name: Provider
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ModelConfig is the Schema for the modelconfigs API.
          It references a Model and defines specific parameters for a use case (e.g., an agent).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelConfigSpec defines the desired state of ModelConfig
            properties:
              anthropic:
                description: Anthropic-specific parameters (includes base ModelParameters
                  via embedding)
                properties:
                  anthropicCacheInstructions:
                    description: |-
                      AnthropicCacheInstructions adds cache_control to the last system prompt block.
                      When true, uses TTL='5m'. Can also specify '5m' or '1h' directly.
                      See https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching
                    type: string
                  anthropicCacheMessages:
                    description: |-
                      AnthropicCacheMessages adds a cache point to the last content block in the final user message.
                      Useful for caching conversation history in multi-turn conversations.
                      When true, uses TTL='5m'. Can also specify '5m' or '1h' directly.
                      Note: Uses 1 of Anthropic's 4 available cache points per request.
                      See https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching
                    type: string
                  anthropicCacheToolDefinitions:
                    description: |-
                      AnthropicCacheToolDefinitions adds cache_control to the last tool definition.
                      When true, uses TTL='5m'. Can also specify '5m' or '1h' directly.
                      See https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching
                    type: string
                  anthropicContainer:
                    description: |-
                      AnthropicContainer configures container for multi-turn conversations.
                      By default, if previous messages contain a container_id, it will be reused automatically.
                    properties:
                      disabled:
                        description: Disabled forces a fresh container (ignores any
                          container_id from history)
                        type: boolean
                      id:
                        description: ID is the container ID to use (e.g., "container_xxx")
                        type: string
                    type: object
                  anthropicMetadataUserID:
                    description: AnthropicMetadataUserID is an external identifier
                      for the user associated with the request.
                    type: string
                  anthropicThinking:
                    description: |-
                      AnthropicThinking determines whether the model should generate a thinking block.
                      See https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking
                    properties:
                      budgetTokens:
                        description: |-
                          BudgetTokens is the maximum number of tokens for thinking (required when enabled)
                          Must be >= 1024 and less than maxTokens
                          See https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking
                        format: int32
                        minimum: 1024
                        type: integer
                      type:
                        allOf:
                        - enum:
                          - enabled
                          - disabled
                        - enum:
                          - enabled
                          - disabled
                        default: disabled
                        description: Type enables or disables extended thinking
                        type: string
                    required:
                    - type
                    type: object
                  extraBody:
                    description: ExtraBody to include in all requests
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  extraHeaders:
                    additionalProperties:
                      type: string
                    description: ExtraHeaders to include in all requests
                    type: object
                  frequencyPenalty:
                    description: |-
                      FrequencyPenalty penalizes frequent tokens (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  logitBias:
                    additionalProperties:
                      format: int32
                      type: integer
                    description: Logit Bias
                    type: object
                  maxTokens:
                    description: MaxTokens is the maximum number of tokens to generate
                    format: int32
                    minimum: 1
                    type: integer
                  parallelToolCalls:
                    description: ParallelToolCalls
                    type: boolean
                  presencePenalty:
                    description: |-
                      PresencePenalty penalizes tokens already present in the context (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  seed:
                    description: Seed for deterministic generation (where supported)
                    format: int64
                    type: integer
                  stopSequences:
                    description: StopSequences are sequences where the model will
                      stop generating
                    items:
                      type: string
                    type: array
                  temperature:
                    description: |-
                      Temperature controls randomness in the output (0.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.7", "1.0")
                    pattern: ^(0(\.\d+)?|1(\.0+)?|2(\.0+)?)$
                    type: string
                  timeOut:
                    description: TimeOut in seconds for the model response
                    format: int32
                    minimum: 1
                    type: integer
                  topK:
                    description: TopK limits the number of tokens to consider for
                      each step
                    format: int32
                    minimum: 1
                    type: integer
                  topP:
                    description: |-
                      TopP controls nucleus sampling (0.0 to 1.0)
                      Specified as a string to avoid floating point issues (e.g., "0.9", "0.95")
                    pattern: ^(0(\.\d+)?|1(\.0+)?)$
                    type: string
                type: object
              bedrock:
                description: Bedrock-specific parameters (includes base ModelParameters
                  via embedding)
                properties:
                  bedrockAdditionalModelRequestFields:
                    description: |-
                      BedrockAdditionalModelRequestFields are additional model-specific parameters to include in requests.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/model-parameters.html
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  bedrockAdditionalModelResponseFieldsPaths:
                    description: |-
                      BedrockAdditionalModelResponseFieldsPaths are JSON paths to extract additional fields from model responses.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/model-parameters.html
                    items:
                      type: string
                    type: array
                  bedrockCacheInstructions:
                    description: |-
                      BedrockCacheInstructions adds a cache point after the system prompt blocks.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html
                    type: boolean
                  bedrockCacheMessages:
                    description: |-
                      BedrockCacheMessages adds a cache point to the last content block in the final user message.
                      Useful for caching conversation history in multi-turn conversations.
                      Note: Uses 1 of Bedrock's 4 available cache points per request.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html
                    type: boolean
                  bedrockCacheToolDefinitions:
                    description: |-
                      BedrockCacheToolDefinitions adds a cache point after the last tool definition.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html
                    type: boolean
                  bedrockGuardrailConfig:
                    description: |-
                      BedrockGuardrailConfig defines content moderation and safety settings.
                      See https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_GuardrailConfiguration.html
                    properties:
                      guardrailIdentifier:
                        description: GuardrailIdentifier is the unique identifier
                          of the guardrail
                        type: string
                      guardrailVersion:
                        description: GuardrailVersion is the version of the guardrail
                        type: string
                      trace:
                        description: Trace controls guardrail tracing level
                        enum:
                        - disabled
                        - enabled
                        - enabled_full
                        type: string
                    type: object
                  bedrockPerformanceConfiguration:
                    description: |-
                      BedrockPerformanceConfiguration defines performance optimization settings.
                      See https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_PerformanceConfiguration.html
                    properties:
                      latency:
                        description: Latency controls performance/cost tradeoff
                        enum:
                        - optimized
                        - standard
                        type: string
                    type: object
                  bedrockPromptVariables:
                    additionalProperties:
                      type: string
                    description: |-
                      BedrockPromptVariables are variables for substitution into prompt templates.
                      Each key maps to a text value.
                      See https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_PromptVariableValues.html
                    type: object
                  bedrockRequestMetadata:
                    additionalProperties:
                      type: string
                    description: BedrockRequestMetadata is additional metadata to
                      attach to Bedrock API requests.
                    type: object
                  bedrockServiceTier:
                    description: |-
                      BedrockServiceTier controls performance and cost optimization.
                      See https://docs.aws.amazon.com/bedrock/latest/userguide/service-tiers-inference.html
                    properties:
                      type:
                        description: Type specifies the service tier type
                        enum:
                        - default
                        - flex
                        - priority
                        - reserved
                        type: string
                    type: object
                  extraBody:
                    description: ExtraBody to include in all requests
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  extraHeaders:
                    additionalProperties:
                      type: string
                    description: ExtraHeaders to include in all requests
                    type: object
                  frequencyPenalty:
                    description: |-
                      FrequencyPenalty penalizes frequent tokens (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  logitBias:
                    additionalProperties:
                      format: int32
                      type: integer
                    description: Logit Bias
                    type: object
                  maxTokens:
                    description: MaxTokens is the maximum number of tokens to generate
                    format: int32
                    minimum: 1
                    type: integer
                  parallelToolCalls:
                    description: ParallelToolCalls
                    type: boolean
                  presencePenalty:
                    description: |-
                      PresencePenalty penalizes tokens already present in the context (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  seed:
                    description: Seed for deterministic generation (where supported)
                    format: int64
                    type: integer
                  stopSequences:
                    description: StopSequences are sequences where the model will
                      stop generating
                    items:
                      type: string
                    type: array
                  temperature:
                    description: |-
                      Temperature controls randomness in the output (0.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.7", "1.0")
                    pattern: ^(0(\.\d+)?|1(\.0+)?|2(\.0+)?)$
                    type: string
                  timeOut:
                    description: TimeOut in seconds for the model response
                    format: int32
                    minimum: 1
                    type: integer
                  topK:
                    description: TopK limits the number of tokens to consider for
                      each step
                    format: int32
                    minimum: 1
                    type: integer
                  topP:
                    description: |-
                      TopP controls nucleus sampling (0.0 to 1.0)
                      Specified as a string to avoid floating point issues (e.g., "0.9", "0.95")
                    pattern: ^(0(\.\d+)?|1(\.0+)?)$
                    type: string
                type: object
              google:
                description: Google/Gemini-specific parameters (includes base ModelParameters
                  via embedding)
                properties:
                  extraBody:
                    description: ExtraBody to include in all requests
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  extraHeaders:
                    additionalProperties:
                      type: string
                    description: ExtraHeaders to include in all requests
                    type: object
                  frequencyPenalty:
                    description: |-
                      FrequencyPenalty penalizes frequent tokens (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  googleCachedContent:
                    description: |-
                      GoogleCachedContent is the name of the cached content to use for the model.
                      See https://ai.google.dev/gemini-api/docs/caching for more information.
                    type: string
                  googleLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      GoogleLabels are user-defined metadata to break down billed charges.
                      Only supported by the Vertex AI API.
                      See https://cloud.google.com/vertex-ai/generative-ai/docs/multimodal/add-labels-to-api-calls
                    type: object
                  googleSafetySettings:
                    description: |-
                      GoogleSafetySettings configures content safety filters.
                      See https://ai.google.dev/gemini-api/docs/safety-settings for more information.
                    items:
                      description: GoogleSafetySetting defines a safety setting for
                        Google/Gemini models
                      properties:
                        category:
                          description: Category is the harm category
                          enum:
                          - HARM_CATEGORY_HARASSMENT
                          - HARM_CATEGORY_HATE_SPEECH
                          - HARM_CATEGORY_SEXUALLY_EXPLICIT
                          - HARM_CATEGORY_DANGEROUS_CONTENT
                          - HARM_CATEGORY_CIVIC_INTEGRITY
                          type: string
                        method:
                          description: |-
                            Method specifies if the threshold is used for probability or severity score.
                            If not specified, the threshold is used for probability score.
                          enum:
                          - SEVERITY
                          - PROBABILITY
                          type: string
                        threshold:
                          description: Threshold is the blocking threshold
                          enum:
                          - BLOCK_NONE
                          - BLOCK_LOW_AND_ABOVE
                          - BLOCK_MEDIUM_AND_ABOVE
                          - BLOCK_ONLY_HIGH
                          - "OFF"
                          type: string
                      required:
                      - category
                      - threshold
                      type: object
                    type: array
                  googleThinkingConfig:
                    description: |-
                      GoogleThinkingConfig configures the thinking behavior for the model.
                      See https://ai.google.dev/gemini-api/docs/thinking for more information.
                    properties:
                      includeThoughts:
                        description: |-
                          IncludeThoughts indicates whether to include thoughts in the response.
                          If true, thoughts are returned only if the model supports thought and thoughts are available.
                        type: boolean
                      thinkingBudget:
                        description: |-
                          ThinkingBudget is the thinking budget in tokens.
                          0 means DISABLED, -1 means AUTOMATIC.
                          The default values and allowed ranges are model dependent.
                        format: int32
                        type: integer
                      thinkingLevel:
                        description: ThinkingLevel controls the amount of thinking
                          tokens the model should generate.
                        enum:
                        - unspecified
                        - minimal
                        - low
                        - medium
                        - high
                        type: string
                    type: object
                  googleVideoResolution:
                    description: |-
                      GoogleVideoResolution specifies the video resolution to use for the model.
                      See https://ai.google.dev/api/generate-content#MediaResolution for more information.
                    enum:
                    - unspecified
                    - low
                    - medium
                    - high
                    type: string
                  logitBias:
                    additionalProperties:
                      format: int32
                      type: integer
                    description: Logit Bias
                    type: object
                  maxTokens:
                    description: MaxTokens is the maximum number of tokens to generate
                    format: int32
                    minimum: 1
                    type: integer
                  parallelToolCalls:
                    description: ParallelToolCalls
                    type: boolean
                  presencePenalty:
                    description: |-
                      PresencePenalty penalizes tokens already present in the context (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  seed:
                    description: Seed for deterministic generation (where supported)
                    format: int64
                    type: integer
                  stopSequences:
                    description: StopSequences are sequences where the model will
                      stop generating
                    items:
                      type: string
                    type: array
                  temperature:
                    description: |-
                      Temperature controls randomness in the output (0.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.7", "1.0")
                    pattern: ^(0(\.\d+)?|1(\.0+)?|2(\.0+)?)$
                    type: string
                  timeOut:
                    description: TimeOut in seconds for the model response
                    format: int32
                    minimum: 1
                    type: integer
                  topK:
                    description: TopK limits the number of tokens to consider for
                      each step
                    format: int32
                    minimum: 1
                    type: integer
                  topP:
                    description: |-
                      TopP controls nucleus sampling (0.0 to 1.0)
                      Specified as a string to avoid floating point issues (e.g., "0.9", "0.95")
                    pattern: ^(0(\.\d+)?|1(\.0+)?)$
                    type: string
                type: object
              modelRef:
                description: ModelRef references the Model resource to use
                properties:
                  name:
                    description: Name of the Model resource
                    type: string
                  namespace:
                    description: Namespace of the Model (defaults to ModelConfig's
                      namespace)
                    type: string
                required:
                - name
                type: object
              openai:
                description: OpenAI-specific parameters (includes base ModelParameters
                  via embedding)
                properties:
                  extraBody:
                    description: ExtraBody to include in all requests
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  extraHeaders:
                    additionalProperties:
                      type: string
                    description: ExtraHeaders to include in all requests
                    type: object
                  frequencyPenalty:
                    description: |-
                      FrequencyPenalty penalizes frequent tokens (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  logProbs:
                    description: LogProbs
                    type: boolean
                  logitBias:
                    additionalProperties:
                      format: int32
                      type: integer
                    description: Logit Bias
                    type: object
                  maxTokens:
                    description: MaxTokens is the maximum number of tokens to generate
                    format: int32
                    minimum: 1
                    type: integer
                  openAIIncludeCodeExecutionOutputs:
                    description: |-
                      OpenAIIncludeCodeExecutionOutputs includes code execution results in the response.
                      Corresponds to the code_interpreter_call.outputs value of the include parameter.
                    type: boolean
                  openAIIncludeFileSearchResults:
                    description: |-
                      OpenAIIncludeFileSearchResults includes file search results in the response.
                      Corresponds to the file_search_call.results value of the include parameter.
                    type: boolean
                  openAIIncludeRawAnnotations:
                    description: |-
                      OpenAIIncludeRawAnnotations includes raw annotations (e.g., citations from web search)
                      in TextPart.provider_details['annotations'].
                    type: boolean
                  openAIIncludeWebSearchSources:
                    description: |-
                      OpenAIIncludeWebSearchSources includes web search results in the response.
                      Corresponds to the web_search_call.action.sources value of the include parameter.
                    type: boolean
                  openAIPreviousResponseID:
                    description: |-
                      OpenAIPreviousResponseID is the ID of a previous response to continue from.
                      When set to "auto", automatically uses the most recent provider_response_id
                      from message history and omits earlier messages. Enables server-side
                      conversation state and faithful reference to previous reasoning.
                    type: string
                  openAIPromptCacheKey:
                    type: string
                  openAIPromptRetention:
                    description: OpenAIPromptRetention controls prompt caching retention
                    type: string
                  openAIReasoningGenerateSummary:
                    description: OpenAIReasoningGenerateSummary
                    enum:
                    - detailed
                    - concise
                    type: string
                  openAIReasoningSummary:
                    description: OpenAIReasoningSummary
                    enum:
                    - detailed
                    - concise
                    - auto
                    type: string
                  openAISendReasoningIDs:
                    description: OpenAISendReasoningIDs
                    type: boolean
                  openAIServiceTier:
                    description: OpenAIServiceTier
                    enum:
                    - auto
                    - default
                    - flex
                    - priority
                    type: string
                  openAITextVerbosity:
                    description: OpenAITextVerbosity
                    enum:
                    - low
                    - medium
                    - high
                    type: string
                  openAITruncation:
                    description: OpenAITruncation
                    enum:
                    - disabled
                    - auto
                    type: string
                  openAIUser:
                    description: OpenAIUser associated with the requests
                    type: string
                  parallelToolCalls:
                    description: ParallelToolCalls
                    type: boolean
                  presencePenalty:
                    description: |-
                      PresencePenalty penalizes tokens already present in the context (-2.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.5", "-1.0")
                    pattern: ^(-2(\.0+)?|-1(\.\d+)?|0(\.0+)?|1(\.\d+)?|2(\.0+)?)$
                    type: string
                  reasoningEffort:
                    description: ReasoningEffort controls the reasoning effort for
                      reasoning models (o1, o3, etc.)
                    enum:
                    - none
                    - minimal
                    - low
                    - medium
                    - high
                    - xhigh
                    type: string
                  responseFormat:
                    description: ResponseFormat specifies the output format
                    properties:
                      jsonSchema:
                        description: JSONSchema for structured outputs (when type
                          is "json_schema")
                        properties:
                          description:
                            description: Description of the schema
                            type: string
                          name:
                            description: Name of the schema
                            type: string
                          schema:
                            description: Schema is the JSON schema definition
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          strict:
                            description: Strict enables strict schema validation
                            type: boolean
                        required:
                        - name
                        type: object
                      type:
                        description: 'Type of response format: "text" or "json_object"'
                        enum:
                        - text
                        - json_object
                        - json_schema
                        type: string
                    required:
                    - type
                    type: object
                  seed:
                    description: Seed for deterministic generation (where supported)
                    format: int64
                    type: integer
                  stopSequences:
                    description: StopSequences are sequences where the model will
                      stop generating
                    items:
                      type: string
                    type: array
                  temperature:
                    description: |-
                      Temperature controls randomness in the output (0.0 to 2.0)
                      Specified as a string to avoid floating point issues (e.g., "0.7", "1.0")
                    pattern: ^(0(\.\d+)?|1(\.0+)?|2(\.0+)?)$
                    type: string
                  timeOut:
                    description: TimeOut in seconds for the model response
                    format: int32
                    minimum: 1
                    type: integer
                  topK:
                    description: TopK limits the number of tokens to consider for
                      each step
                    format: int32
                    minimum: 1
                    type: integer
                  topLogProbs:
                    description: TopLogProbs
                    format: int32
                    type: integer
                  topP:
                    description: |-
                      TopP controls nucleus sampling (0.0 to 1.0)
                      Specified as a string to avoid floating point issues (e.g., "0.9", "0.95")
                    pattern: ^(0(\.\d+)?|1(\.0+)?)$
                    type: string
                type: object
            required:
            - modelRef
            type: object
          status:
            description: ModelConfigStatus defines the observed state of ModelConfig
            properties:
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Observed generation
                format: int64
                type: integer
              ready:
                description: Ready indicates if the model config is ready to be used
                type: boolean
              resolvedModel:
                description: ResolvedModel contains the resolved model information
                properties:
                  model:
                    description: Model name from the referenced Model
                    type: string
                  name:
                    description: Name of the referenced Model
                    type: string
                  namespace:
                    description: Namespace of the referenced Model
                    type: string
                  provider:
                    description: Provider from the referenced Model
                    enum:
                    - openai
                    - anthropic
                    - google
                    - bedrock
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
