---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modelproviders.agent.flokoa.ai
spec:
  group: agent.flokoa.ai
  names:
    kind: ModelProvider
    listKind: ModelProviderList
    plural: modelproviders
    singular: modelprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.provider
      name: Provider
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ModelProvider is the Schema for the modelproviders API.
          It defines connection configuration for an LLM provider (OpenAI, Anthropic, Google, Bedrock).
          The provider type is inferred from which configuration block is set.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ModelProviderSpec defines the desired state of ModelProvider.
              Exactly one of OpenAI, Anthropic, Google, or Bedrock must be specified.
              The provider type is inferred from which block is set.
            properties:
              anthropic:
                description: Anthropic-specific configuration. If set, provider is
                  Anthropic.
                properties:
                  baseURL:
                    description: BaseURL overrides the default Anthropic API endpoint
                    type: string
                  timeoutSeconds:
                    default: 60
                    description: Timeout in seconds for API requests
                    format: int32
                    type: integer
                type: object
              apiKeySecretRef:
                description: |-
                  APIKeySecretRef references a secret containing the API key
                  The secret should have a key named "api-key" unless specified otherwise
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              bedrock:
                description: Bedrock-specific configuration. If set, provider is AWS
                  Bedrock.
                properties:
                  region:
                    description: Region is the AWS region
                    type: string
                type: object
              defaultHeaders:
                additionalProperties:
                  type: string
                description: DefaultHeaders to include in all requests
                type: object
              google:
                description: Google-specific configuration. If set, provider is Google/Gemini.
                properties:
                  location:
                    description: Location is the Google Cloud region (e.g., "us-central1",
                      for Vertex AI)
                    type: string
                  project:
                    description: Project is the Google Cloud project ID (for Vertex
                      AI)
                    type: string
                  serviceAccountKeySecretRef:
                    description: ServiceAccountKeySecretRef references a secret containing
                      the service account JSON key
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  timeoutSeconds:
                    default: 60
                    description: Timeout in seconds for API requests
                    format: int32
                    type: integer
                type: object
              openai:
                description: OpenAI-specific configuration. If set, provider is OpenAI.
                properties:
                  baseURL:
                    description: BaseURL overrides the default OpenAI API endpoint
                    type: string
                  timeoutSeconds:
                    default: 60
                    description: Timeout in seconds for API requests
                    format: int32
                    type: integer
                type: object
              tls:
                description: TLS configuration for custom endpoints
                properties:
                  caSecretRef:
                    description: |-
                      CASecretRef references a secret containing a custom CA certificate
                      The secret should have a key named "ca.crt"
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  insecureSkipVerify:
                    description: InsecureSkipVerify disables TLS certificate verification
                    type: boolean
                  useSystemCAs:
                    default: true
                    description: UseSystemCAs includes system CA certificates in addition
                      to any custom CA
                    type: boolean
                type: object
            type: object
          status:
            description: ModelProviderStatus defines the observed state of ModelProvider
            properties:
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Observed generation
                format: int64
                type: integer
              provider:
                description: Provider is the resolved provider type (inferred from
                  which spec block is set)
                enum:
                - openai
                - anthropic
                - google
                - bedrock
                type: string
              ready:
                description: Ready indicates if the model provider is ready to be
                  used
                type: boolean
              secretHash:
                description: SecretHash is a hash of the referenced secrets for change
                  detection
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
