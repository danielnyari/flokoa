apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app.kubernetes.io/name: flokoa-server
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app.kubernetes.io/name: flokoa-server
