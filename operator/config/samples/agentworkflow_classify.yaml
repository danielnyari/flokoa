apiVersion: agent.flokoa.ai/v1alpha1
kind: AgentWorkflow
metadata:
  name: classify-sentiment
spec:
  description: "Classify customer feedback sentiment using Marvin"
  tasks:
    - name: classify
      agentTask:
        type: classify
        input: "This product exceeded all my expectations!"
        labels: ["positive", "negative", "neutral"]
