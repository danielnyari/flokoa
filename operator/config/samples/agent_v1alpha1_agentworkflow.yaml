apiVersion: agent.flokoa.ai/v1alpha1
kind: AgentWorkflow
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: agentworkflow-sample
spec:
  description: "Sample workflow calling a deployed Agent via A2A"
  tasks:
    - name: summarize
      agent:
        name: agent-sample
        text: "Summarize the latest platform changes in three bullet points."
