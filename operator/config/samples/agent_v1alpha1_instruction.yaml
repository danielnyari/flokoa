apiVersion: agent.flokoa.ai/v1alpha1
kind: Instruction
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: instruction-sample
spec:
  # TODO(user): Add fields here
