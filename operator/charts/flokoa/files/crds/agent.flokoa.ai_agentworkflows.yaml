---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: agentworkflows.agent.flokoa.ai
spec:
  group: agent.flokoa.ai
  names:
    kind: AgentWorkflow
    listKind: AgentWorkflowList
    plural: agentworkflows
    shortNames:
    - awf
    singular: agentworkflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.workflowTemplateName
      name: Template
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AgentWorkflow is the Schema for the agentworkflows API.
          It defines a declarative, agent-native workflow for orchestrating AI agent tasks.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentWorkflowSpec defines the desired state of AgentWorkflow.
            properties:
              automountServiceAccountToken:
                default: true
                description: |-
                  AutomountServiceAccountToken controls whether the service account token
                  is automatically mounted into workflow pods. Defaults to true.
                  Argo executor plugins require this to be true.
                type: boolean
              description:
                description: Description is a human-readable description of the workflow.
                type: string
              params:
                description: Params are workflow-level parameters that can be referenced
                  in expressions.
                items:
                  description: WorkflowParam defines a workflow-level parameter.
                  properties:
                    description:
                      description: Description of what this parameter is used for.
                      type: string
                    name:
                      description: Name of the parameter.
                      minLength: 1
                      type: string
                    value:
                      description: |-
                        Value is the default parameter value. If empty, the parameter must be
                        provided at workflow submission time.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              retryStrategy:
                description: |-
                  RetryStrategy is the default retry policy for all tasks.
                  Individual tasks can override this.
                properties:
                  backoff:
                    description: Backoff configures the backoff strategy between retries.
                    properties:
                      duration:
                        description: Duration is the initial backoff duration (e.g.,
                          "30s", "5m").
                        type: string
                      factor:
                        description: Factor is the multiplier applied to the duration
                          after each retry.
                        format: int32
                        type: integer
                    required:
                    - duration
                    type: object
                  limit:
                    description: Limit is the maximum number of retry attempts.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - limit
                type: object
              serviceAccountName:
                default: flokoa-workflow
                description: |-
                  ServiceAccountName is the Kubernetes ServiceAccount used for workflow pods.
                  Defaults to "flokoa-workflow" if not specified.
                type: string
              tasks:
                description: Tasks defines the workflow tasks to execute.
                items:
                  description: |-
                    WorkflowTask defines a single task in the workflow.
                    Exactly one of Agent, AgentTask, Container, HTTP, or Switch must be specified.
                  properties:
                    agent:
                      description: Agent calls a deployed Agent CR via the A2A protocol.
                      properties:
                        config:
                          description: Config configures how the message is sent to
                            the agent.
                          properties:
                            acceptedOutputModes:
                              description: AcceptedOutputModes restricts the agent's
                                output format.
                              items:
                                type: string
                              type: array
                            blocking:
                              description: Blocking indicates whether to wait synchronously
                                for task completion.
                              type: boolean
                            historyLength:
                              description: HistoryLength limits the conversation history
                                returned with the response.
                              format: int32
                              type: integer
                            pushNotificationConfig:
                              description: |-
                                PushNotificationConfig configures push notifications for async task updates.
                                Aligns with a2a.MessageSendConfig.PushNotificationConfig.
                              properties:
                                authentication:
                                  description: Authentication configures authentication
                                    for push notification delivery.
                                  properties:
                                    credentials:
                                      description: Credentials is the authentication
                                        credential (e.g., a bearer token).
                                      type: string
                                    schemes:
                                      description: Schemes lists the authentication
                                        schemes supported (e.g., "Bearer").
                                      items:
                                        type: string
                                      minItems: 1
                                      type: array
                                  required:
                                  - schemes
                                  type: object
                                id:
                                  description: ID is an optional identifier for this
                                    push notification configuration.
                                  type: string
                                token:
                                  description: Token is an opaque token the agent
                                    includes in push notifications for verification.
                                  type: string
                                url:
                                  description: URL is the webhook endpoint to receive
                                    push notifications.
                                  minLength: 1
                                  type: string
                              required:
                              - url
                              type: object
                          type: object
                        message:
                          description: |-
                            Message is the full A2A message to send to the agent.
                            Mutually exclusive with Text. Use this for multi-part messages,
                            file attachments, or advanced message options.
                          properties:
                            contextId:
                              description: |-
                                ContextID for conversation threading across agent interactions.
                                Aligns with a2a.Message.ContextID.
                              type: string
                            extensions:
                              description: |-
                                Extensions lists A2A extension URIs to activate for this message.
                                Aligns with a2a.Message.Extensions.
                              items:
                                type: string
                              type: array
                            metadata:
                              additionalProperties:
                                x-kubernetes-preserve-unknown-fields: true
                              description: |-
                                Metadata is arbitrary key-value metadata attached to the message.
                                Aligns with a2a.Message.Metadata.
                              type: object
                            parts:
                              description: Parts is the content of the message. At
                                least one part is required.
                              items:
                                description: |-
                                  MessagePart represents a content part in an A2A message.
                                  Exactly one of Text, Data, or File must be set.
                                  Aligns with a2a.Part (TextPart, DataPart, FilePart) from github.com/a2aproject/a2a-go.
                                properties:
                                  data:
                                    description: Data content part (structured JSON).
                                    properties:
                                      data:
                                        description: Data is arbitrary JSON data to
                                          send to the agent.
                                        x-kubernetes-preserve-unknown-fields: true
                                      metadata:
                                        additionalProperties:
                                          x-kubernetes-preserve-unknown-fields: true
                                        description: Metadata for this part.
                                        type: object
                                    required:
                                    - data
                                    type: object
                                  file:
                                    description: File content part.
                                    properties:
                                      file:
                                        description: File content (either inline bytes
                                          or a URI reference).
                                        properties:
                                          bytes:
                                            description: |-
                                              Bytes is the base64-encoded file content.
                                              Exactly one of Bytes or URI must be set.
                                            type: string
                                          mimeType:
                                            description: MimeType of the file content.
                                            type: string
                                          name:
                                            description: Name of the file.
                                            type: string
                                          uri:
                                            description: |-
                                              URI is a reference to the file location.
                                              Exactly one of Bytes or URI must be set.
                                            type: string
                                        type: object
                                      metadata:
                                        additionalProperties:
                                          x-kubernetes-preserve-unknown-fields: true
                                        description: Metadata for this part.
                                        type: object
                                    required:
                                    - file
                                    type: object
                                  text:
                                    description: Text content part.
                                    properties:
                                      metadata:
                                        additionalProperties:
                                          x-kubernetes-preserve-unknown-fields: true
                                        description: Metadata for this part.
                                        type: object
                                      text:
                                        description: Text content. Supports expressions
                                          like {{params.topic}} or {{tasks.prev.output}}.
                                        type: string
                                    required:
                                    - text
                                    type: object
                                type: object
                              minItems: 1
                              type: array
                            referenceTaskIds:
                              description: |-
                                ReferenceTaskIDs links this message to previous A2A task IDs.
                                Aligns with a2a.Message.ReferenceTasks.
                              items:
                                type: string
                              type: array
                            role:
                              default: user
                              description: Role of the message sender.
                              enum:
                              - user
                              - agent
                              type: string
                            taskId:
                              description: |-
                                TaskID continues an existing A2A task instead of creating a new one.
                                Aligns with a2a.Message.TaskID.
                              type: string
                          required:
                          - parts
                          type: object
                        name:
                          description: Name is the name of the Agent CR to call.
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace of the Agent CR.
                            Defaults to the workflow namespace.
                          type: string
                        text:
                          description: |-
                            Text is a shorthand for sending a simple text message.
                            Mutually exclusive with Message. When set, a single-part
                            AgentMessage with this text is constructed automatically.
                            Supports expressions like {{params.topic}} or {{tasks.prev.output}}.
                          type: string
                      required:
                      - name
                      type: object
                    agentTask:
                      description: AgentTask runs a Marvin-powered task in an ephemeral
                        container.
                      properties:
                        agent:
                          description: |-
                            Agent defines an inline Marvin agent for execution.
                            Maps to the agent parameter in marvin.run(), marvin.classify(), etc.
                          properties:
                            instructions:
                              description: Instructions are behavioral instructions
                                for this agent.
                              type: string
                            model:
                              description: |-
                                Model references a Model CR for this agent's LLM.
                                If not specified, inherits the task-level model.
                              properties:
                                name:
                                  description: Name of the Model resource.
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace of the Model resource. Defaults
                                    to the Agent's namespace if not specified.
                                  type: string
                              required:
                              - name
                              type: object
                            name:
                              description: Name of the agent.
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        context:
                          additionalProperties:
                            type: string
                          description: |-
                            Context is key-value data passed to the Marvin task.
                            Values support expressions like {{tasks.prev.output}}.
                          type: object
                        count:
                          description: |-
                            Count is the number of items to generate.
                            Maps to marvin.generate(n=...).
                            Only used when type=generate. Defaults to 1.
                          format: int32
                          minimum: 1
                          type: integer
                        env:
                          description: Env is additional environment variables for
                            the container.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: Name of the environment variable. Must
                                  be a C_IDENTIFIER.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        image:
                          description: Image overrides the container image. Defaults
                            to the flokoa managed-task runtime.
                          type: string
                        input:
                          description: |-
                            Input is the data to process.
                            - classify: data to classify (required)
                            - extract: data to extract from (required)
                            - cast: data to transform (required)
                            - run/generate: not used
                            Supports expressions like {{tasks.prev.output}}.
                          type: string
                        instruction:
                          description: |-
                            Instruction provides the prompt or guidance for the task.
                            - run: the main prompt (required)
                            - classify/extract/cast/generate: optional guidance instructions
                            Supports inline text (template) or reference to an Instruction CR (instructionRef).
                          properties:
                            instructionRef:
                              description: InstructionRef references an existing Instruction
                                resource.
                              properties:
                                name:
                                  description: Name of the resource.
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace of the resource. Defaults
                                    to the referencing resource's namespace if not
                                    specified.
                                  type: string
                              required:
                              - name
                              type: object
                            template:
                              description: |-
                                Template defines the instruction content directly in the Agent spec.
                                When set, the operator creates a child Instruction CR.
                              type: string
                          type: object
                        labels:
                          description: |-
                            Labels for classify operations.
                            Maps to marvin.classify(labels=[...]).
                            Required when type=classify.
                          items:
                            type: string
                          type: array
                        model:
                          description: Model references a Model CR for the LLM configuration.
                          properties:
                            name:
                              description: Name of the Model resource.
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace of the Model resource. Defaults
                                to the Agent's namespace if not specified.
                              type: string
                          required:
                          - name
                          type: object
                        multiLabel:
                          description: |-
                            MultiLabel enables multi-label classification (returns list instead of single label).
                            Maps to marvin.classify(multi_label=True).
                            Only used when type=classify.
                          type: boolean
                        resources:
                          description: Resources specifies compute resource requirements
                            for the container.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This is an alpha field and requires enabling the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        resultType:
                          description: |-
                            ResultType constrains the output via JSON Schema.
                            - run: optional (defaults to str)
                            - extract: target type (defaults to str)
                            - cast: target type (required)
                            - generate: target type (required)
                            - classify: not used (labels define the output)
                          properties:
                            description:
                              type: string
                            jsonSchema:
                              x-kubernetes-preserve-unknown-fields: true
                            name:
                              type: string
                          required:
                          - description
                          - jsonSchema
                          - name
                          type: object
                        tools:
                          description: |-
                            Tools available to the task. Can be inline definitions or references to AgentTool CRs.
                            Primarily used with type=run.
                          items:
                            description: |-
                              ToolEntry represents either an inline tool definition or a reference to an AgentTool resource.
                              Exactly one of Inline or ToolRef must be specified.
                            properties:
                              name:
                                description: Name of the tool. Required for inline
                                  tools, used as identifier.
                                minLength: 1
                                type: string
                              template:
                                description: |-
                                  Template defines the tool directly in the Agent spec.
                                  Uses the same spec as AgentTool for consistency.
                                properties:
                                  description:
                                    description: Description is a human-readable description
                                      for the LLM to understand the tool's purpose.
                                    minLength: 1
                                    type: string
                                  openApi:
                                    description: |-
                                      OpenApi contains OpenAPI tool configuration.
                                      Required when Type is "openapi".
                                    properties:
                                      headers:
                                        additionalProperties:
                                          type: string
                                        description: Headers are additional HTTP headers
                                          to include in API requests.
                                        type: object
                                      openApiSchema:
                                        description: |-
                                          OpenApiSchema specifies the source of the OpenAPI specification.
                                          Exactly one of Value, ValueFrom, or EndpointPath must be specified.
                                        properties:
                                          endpointPath:
                                            description: |-
                                              EndpointPath is a path on the target service/URL where the OpenAPI spec is served
                                              (e.g., "/openapi.json", "/docs/openapi.json", "/swagger.json").
                                              The operator or runtime will fetch the spec from this path on the target.
                                            type: string
                                          value:
                                            description: Value contains the OpenAPI
                                              specification inline.
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                          valueFrom:
                                            description: ValueFrom references a ConfigMap
                                              containing the OpenAPI specification.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                default: ""
                                                description: |-
                                                  Name of the referent.
                                                  This field is effectively required, but due to backwards compatibility is
                                                  allowed to be empty. Instances of this type with an empty value here are
                                                  almost certainly wrong.
                                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                                type: string
                                              optional:
                                                description: Specify whether the ConfigMap
                                                  or its key must be defined
                                                type: boolean
                                            required:
                                            - key
                                            type: object
                                            x-kubernetes-map-type: atomic
                                        type: object
                                      serviceRef:
                                        description: |-
                                          ServiceRef references a Kubernetes service as the target API.
                                          Mutually exclusive with URL.
                                        properties:
                                          name:
                                            description: Name is the service name.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace is the service
                                              namespace. Defaults to the AgentTool's
                                              namespace.
                                            type: string
                                          port:
                                            description: |-
                                              Port is the service port number.
                                              Mutually exclusive with PortName.
                                            format: int32
                                            maximum: 65535
                                            minimum: 1
                                            type: integer
                                          portName:
                                            description: |-
                                              PortName is the service port name.
                                              Mutually exclusive with Port.
                                            type: string
                                        required:
                                        - name
                                        type: object
                                      timeoutSeconds:
                                        default: 30
                                        description: TimeoutSeconds is the request
                                          timeout in seconds for API calls.
                                        format: int32
                                        maximum: 300
                                        minimum: 1
                                        type: integer
                                      url:
                                        description: |-
                                          URL is the base URL of the external API.
                                          Mutually exclusive with ServiceRef.
                                        type: string
                                    required:
                                    - openApiSchema
                                    type: object
                                  type:
                                    description: Type specifies the kind of tool (e.g.,
                                      openapi).
                                    enum:
                                    - openapi
                                    type: string
                                required:
                                - description
                                - type
                                type: object
                              toolRef:
                                description: ToolRef references an existing AgentTool
                                  resource.
                                properties:
                                  name:
                                    description: Name of the AgentTool resource.
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: Namespace of the AgentTool. Defaults
                                      to the Agent's namespace if not specified.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                          type: array
                        type:
                          description: 'Type is the Marvin operation: run, classify,
                            extract, cast, or generate.'
                          enum:
                          - run
                          - classify
                          - extract
                          - cast
                          - generate
                          type: string
                      required:
                      - type
                      type: object
                    condition:
                      description: |-
                        Condition is an expression that must evaluate to true for this task to run.
                        If false, the task is skipped.
                      type: string
                    container:
                      description: |-
                        Container runs an arbitrary container workload.
                        The container should write its result to /tmp/result and optional
                        structured output to /tmp/artifact.
                      properties:
                        args:
                          description: Args are arguments to the command.
                          items:
                            type: string
                          type: array
                        command:
                          description: Command is the entrypoint command.
                          items:
                            type: string
                          type: array
                        env:
                          description: |-
                            Env is additional environment variables for the container.
                            Values support DSL expressions like {{params.x}} or {{tasks.prev.output}}.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: Name of the environment variable. Must
                                  be a C_IDENTIFIER.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        image:
                          description: Image is the container image to run.
                          minLength: 1
                          type: string
                        resources:
                          description: Resources specifies compute resource requirements
                            for the container.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This is an alpha field and requires enabling the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        volumeMounts:
                          description: VolumeMounts are volume mounts for the container.
                          items:
                            description: VolumeMount describes a mounting of a Volume
                              within a container.
                            properties:
                              mountPath:
                                description: |-
                                  Path within the container at which the volume should be mounted.  Must
                                  not contain ':'.
                                type: string
                              mountPropagation:
                                description: |-
                                  mountPropagation determines how mounts are propagated from the host
                                  to container and the other way around.
                                  When not set, MountPropagationNone is used.
                                  This field is beta in 1.10.
                                  When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                  (which defaults to None).
                                type: string
                              name:
                                description: This must match the Name of a Volume.
                                type: string
                              readOnly:
                                description: |-
                                  Mounted read-only if true, read-write otherwise (false or unspecified).
                                  Defaults to false.
                                type: boolean
                              recursiveReadOnly:
                                description: |-
                                  RecursiveReadOnly specifies whether read-only mounts should be handled
                                  recursively.

                                  If ReadOnly is false, this field has no meaning and must be unspecified.

                                  If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                  recursively read-only.  If this field is set to IfPossible, the mount is made
                                  recursively read-only, if it is supported by the container runtime.  If this
                                  field is set to Enabled, the mount is made recursively read-only if it is
                                  supported by the container runtime, otherwise the pod will not be started and
                                  an error will be generated to indicate the reason.

                                  If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                  None (or be unspecified, which defaults to None).

                                  If this field is not specified, it is treated as an equivalent of Disabled.
                                type: string
                              subPath:
                                description: |-
                                  Path within the volume from which the container's volume should be mounted.
                                  Defaults to "" (volume's root).
                                type: string
                              subPathExpr:
                                description: |-
                                  Expanded path within the volume from which the container's volume should be mounted.
                                  Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                  Defaults to "" (volume's root).
                                  SubPathExpr and SubPath are mutually exclusive.
                                type: string
                            required:
                            - mountPath
                            - name
                            type: object
                          type: array
                        workingDir:
                          description: WorkingDir is the working directory inside
                            the container.
                          type: string
                      required:
                      - image
                      type: object
                    dependsOn:
                      description: |-
                        DependsOn lists task names that must complete before this task starts.
                        Defines DAG edges.
                      items:
                        type: string
                      type: array
                    http:
                      description: |-
                        HTTP makes an HTTP request to an external service.
                        The response body is captured as the task output.
                      properties:
                        body:
                          description: Body is the HTTP request body. Supports DSL
                            expressions.
                          type: string
                        headers:
                          description: Headers are HTTP headers to include in the
                            request.
                          items:
                            description: |-
                              HTTPHeader defines an HTTP header with a name and value.
                              Exactly one of Value or ValueFrom must be set.
                            properties:
                              name:
                                description: Name is the header name.
                                minLength: 1
                                type: string
                              value:
                                description: |-
                                  Value is the header value. Supports DSL expressions.
                                  Mutually exclusive with ValueFrom.
                                type: string
                              valueFrom:
                                description: |-
                                  ValueFrom references a secret or configmap key for the header value.
                                  Mutually exclusive with Value.
                                properties:
                                  configMapKeyRef:
                                    description: ConfigMapKeyRef references a key
                                      in a Kubernetes ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: SecretKeyRef references a key in
                                      a Kubernetes Secret.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        method:
                          default: GET
                          description: Method is the HTTP method to use.
                          enum:
                          - GET
                          - POST
                          - PUT
                          - DELETE
                          - PATCH
                          - HEAD
                          - OPTIONS
                          type: string
                        successCondition:
                          description: |-
                            SuccessCondition is an Argo-native expression evaluated against the HTTP response
                            to determine if the request was successful (e.g., "response.statusCode == 200").
                          type: string
                        url:
                          description: URL is the HTTP request URL. Supports DSL expressions.
                          minLength: 1
                          type: string
                      required:
                      - url
                      type: object
                    name:
                      description: Name is the unique identifier for this task within
                        the workflow.
                      minLength: 1
                      pattern: ^[a-zA-Z0-9][-a-zA-Z0-9]*$
                      type: string
                    retryStrategy:
                      description: RetryStrategy overrides the workflow-level retry
                        policy for this task.
                      properties:
                        backoff:
                          description: Backoff configures the backoff strategy between
                            retries.
                          properties:
                            duration:
                              description: Duration is the initial backoff duration
                                (e.g., "30s", "5m").
                              type: string
                            factor:
                              description: Factor is the multiplier applied to the
                                duration after each retry.
                              format: int32
                              type: integer
                          required:
                          - duration
                          type: object
                        limit:
                          description: Limit is the maximum number of retry attempts.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - limit
                      type: object
                    switch:
                      description: Switch routes to different tasks based on the output
                        of a previous task.
                      items:
                        description: |-
                          SwitchCase defines a conditional branch in a switch task.
                          Exactly one of Condition+Then or Default must be set.
                        properties:
                          condition:
                            description: Condition is an expression evaluated against
                              available task outputs.
                            type: string
                          default:
                            description: Default is the fallback task name if no other
                              condition matches.
                            type: string
                          then:
                            description: Then is the task name to run if the condition
                              is true.
                            type: string
                        type: object
                      type: array
                    timeout:
                      description: Timeout is the maximum duration for this task.
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              timeout:
                description: Timeout is the maximum duration for the entire workflow.
                type: string
            required:
            - tasks
            type: object
          status:
            description: |-
              AgentWorkflowStatus defines the observed state of AgentWorkflow.
              The AgentWorkflow compiles to an Argo WorkflowTemplate; individual runs
              are Argo Workflow CRs created from the template and tracked separately.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the workflow's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              ready:
                description: Ready indicates the WorkflowTemplate has been successfully
                  compiled and applied.
                type: boolean
              specHash:
                description: |-
                  SpecHash is a hash of the AgentWorkflow spec, used for drift detection.
                  If the spec has not changed since last reconcile, recompilation is skipped.
                type: string
              workflowTemplateName:
                description: WorkflowTemplateName is the name of the generated Argo
                  WorkflowTemplate CR.
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
