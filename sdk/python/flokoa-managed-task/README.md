# flokoa-managed-task

Managed task runtime for Flokoa operator-deployed agent tasks (Marvin).

This package is built into a container image and deployed by the Flokoa Kubernetes Operator. It is **not** published to PyPI.

**Status: scaffold only — not yet implemented.**
