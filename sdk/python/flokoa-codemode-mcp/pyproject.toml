[project]
name = "flokoa-codemode-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.0.2",
    "flokoa-types",

]


[tool.uv.sources]
flokoa-types = { workspace = true }
