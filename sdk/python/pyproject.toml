[project]
name = "flokoa-workspace"
version = "0.0.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = [
    "flokoa",
    "flokoa-types",
    "flokoa-managed-agent",
    "flokoa-managed-task",
]
