[project]
name = "flokoa-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.3.22",
    "click>=8.3.1",
    "fastapi>=0.128.0",
    "pydantic>=2.12.5",
]

[project.optional-dependencies]
pydantic-ai = [
    "pydantic-ai>=1.44.0",
]

[dependency-groups]
pydantic-ai = [
    "flokoa-sdk[pydantic-ai]",
]
